# spring:
#     config:
#         import: optional:configserver:http://localhost:8888
#     application:
#         name: product-service

server:
  port: 2000
spring:
  data:
    mongodb:
      username: root
      password: root
      host: localhost
      port: 27017
      database: product_db
      authentication-database: admin

